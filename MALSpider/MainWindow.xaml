<Window x:Class="MALSpider.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MALSpider"
        mc:Ignorable="d"
        Title="MALSpider" Height="600" Width="1000" MinHeight="500" MinWidth="850" Background="#121212" Foreground="#E0E0E0" FontFamily="Bahnschrift, Segoe UI, Roboto, Helvetica, Arial, sans-serif"
        TextOptions.TextFormattingMode="Display">
    <Window.Resources>
        <DrawingImage x:Key="SpiderIcon">
            <DrawingImage.Drawing>
                <DrawingGroup>
                    <GeometryDrawing Brush="#BB86FC">
                        <GeometryDrawing.Geometry>
                            <EllipseGeometry Center="16,16" RadiusX="6" RadiusY="8" />
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                    <GeometryDrawing>
                        <GeometryDrawing.Pen>
                            <Pen Brush="#BB86FC" Thickness="2" />
                        </GeometryDrawing.Pen>
                        <GeometryDrawing.Geometry>
                            <GeometryGroup>
                                <LineGeometry StartPoint="10,10" EndPoint="4,4" />
                                <LineGeometry StartPoint="10,16" EndPoint="2,16" />
                                <LineGeometry StartPoint="10,22" EndPoint="4,28" />
                                <LineGeometry StartPoint="22,10" EndPoint="28,4" />
                                <LineGeometry StartPoint="22,16" EndPoint="30,16" />
                                <LineGeometry StartPoint="22,22" EndPoint="28,28" />
                            </GeometryGroup>
                        </GeometryDrawing.Geometry>
                    </GeometryDrawing>
                </DrawingGroup>
            </DrawingImage.Drawing>
        </DrawingImage>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#3700B3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BB86FC"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3700B3"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#444444"/>
                                <Setter Property="Foreground" Value="#888888"/>
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="templateRoot" SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" Width="0"/>
                            </Grid.ColumnDefinitions>
                            <Popup x:Name="PART_Popup" AllowsTransparency="True" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="1" Placement="Bottom" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}">
                                <Border x:Name="dropDownBorder" Background="#2A2A2A" BorderBrush="#444444" BorderThickness="1" CornerRadius="8" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=templateRoot}">
                                    <ScrollViewer x:Name="DropDownScrollViewer">
                                        <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                            <Canvas x:Name="canvas" HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                                <Rectangle x:Name="opaqueRect" Fill="{Binding Background, ElementName=dropDownBorder}" Height="{Binding ActualHeight, ElementName=dropDownBorder}" Width="{Binding ActualWidth, ElementName=dropDownBorder}"/>
                                            </Canvas>
                                            <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                        </Grid>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                            <ToggleButton x:Name="toggleButton" Background="{TemplateBinding Background}" BorderThickness="0" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateRoot" Background="{TemplateBinding Background}" BorderBrush="#444444" BorderThickness="1" CornerRadius="8" SnapsToDevicePixels="True">
                                            <Border x:Name="splitBorder" Width="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" BorderBrush="Transparent" BorderThickness="0" HorizontalAlignment="Right" Margin="0" SnapsToDevicePixels="True">
                                                <Path x:Name="arrow" Data="F1 M 0,0 L 2.667,2.66665 L 5.3334,0 L 5.3334,-0.01041 L 0,-0.01041 L 0,0 Z" Fill="#BB86FC" HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center"/>
                                            </Border>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="templateRoot" Property="BorderBrush" Value="#BB86FC"/>
                                                <Setter TargetName="arrow" Property="Fill" Value="White"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="True">
                                                <Setter TargetName="arrow" Property="Fill" Value="White"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Content="{TemplateBinding SelectionBoxItem}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="False" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            <TextBox x:Name="PART_EditableTextBox" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}}" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Visibility="Hidden">
                                <TextBox.Style>
                                    <Style TargetType="TextBox">
                                        <Setter Property="OverridesDefaultStyle" Value="True"/>
                                        <Setter Property="AllowDrop" Value="True"/>
                                        <Setter Property="MinWidth" Value="0"/>
                                        <Setter Property="MinHeight" Value="0"/>
                                        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
                                        <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
                                        <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
                                        <Setter Property="Foreground" Value="White"/>
                                        <Setter Property="CaretBrush" Value="White"/>
                                        <Setter Property="Template">
                                            <Setter.Value>
                                                <ControlTemplate TargetType="TextBox">
                                                    <ScrollViewer x:Name="PART_ContentHost" Background="Transparent" Focusable="False" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                                                </ControlTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </TextBox.Style>
                            </TextBox>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEditable" Value="True">
                                <Setter Property="IsTabStop" Value="False"/>
                                <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="contentPresenter" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="border" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                            <ContentPresenter Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#333333"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3700B3"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernScrollBarThumb" TargetType="Thumb">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Border x:Name="rectangle"
                                Background="#333333"
                                CornerRadius="5"
                                SnapsToDevicePixels="True"
                                Margin="2"/>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter TargetName="rectangle" Property="Background" Value="#BB86FC"/>
                            </Trigger>
                            <Trigger Property="IsDragging" Value="true">
                                <Setter TargetName="rectangle" Property="Background" Value="#BB86FC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Width" Value="{x:Static local:MALSpiderConstants.ScrollBarSize}"/>
            <Setter Property="MinWidth" Value="{x:Static local:MALSpiderConstants.ScrollBarSize}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="{TemplateBinding Background}">
                            <Track x:Name="PART_Track"
                                   IsDirectionReversed="true"
                                   IsEnabled="{TemplateBinding IsEnabled}">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageUpCommand}" Focusable="false" Opacity="0" />
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="{x:Static ScrollBar.PageDownCommand}" Focusable="false" Opacity="0" />
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource ModernScrollBarThumb}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Horizontal">
                    <Setter Property="Width" Value="Auto"/>
                    <Setter Property="MinWidth" Value="0"/>
                    <Setter Property="Height" Value="{x:Static local:MALSpiderConstants.ScrollBarSize}"/>
                    <Setter Property="MinHeight" Value="{x:Static local:MALSpiderConstants.ScrollBarSize}"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ScrollBar">
                                <Grid x:Name="Bg" SnapsToDevicePixels="true" Background="{TemplateBinding Background}">
                                    <Track x:Name="PART_Track"
                                           IsDirectionReversed="false"
                                           IsEnabled="{TemplateBinding IsEnabled}">
                                        <Track.DecreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.PageLeftCommand}" Focusable="false" Opacity="0" />
                                        </Track.DecreaseRepeatButton>
                                        <Track.IncreaseRepeatButton>
                                            <RepeatButton Command="{x:Static ScrollBar.PageRightCommand}" Focusable="false" Opacity="0" />
                                        </Track.IncreaseRepeatButton>
                                        <Track.Thumb>
                                            <Thumb Style="{StaticResource ModernScrollBarThumb}" />
                                        </Track.Thumb>
                                    </Track>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ProgressBar">
            <Setter Property="Foreground" Value="#BB86FC"/>
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <Border Background="{TemplateBinding Background}" CornerRadius="8"/>
                            <Border x:Name="PART_Indicator" Background="{TemplateBinding Foreground}" HorizontalAlignment="Left" CornerRadius="8"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernToggleSwitch" TargetType="CheckBox">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                            <ContentPresenter HorizontalAlignment="Center" Margin="0,0,0,6"/>
                            <Grid x:Name="ToggleRoot" Width="40" Height="20">
                                <Border x:Name="BackgroundBorder" Background="#333333" CornerRadius="10" BorderThickness="0"/>
                                <Border x:Name="Thumb" Background="White" Width="14" Height="14" CornerRadius="7" HorizontalAlignment="Left" Margin="3">
                                    <Border.RenderTransform>
                                        <TranslateTransform x:Name="ThumbTransform" X="0"/>
                                    </Border.RenderTransform>
                                </Border>
                            </Grid>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="BackgroundBorder" Property="Background" Value="#BB86FC"/>
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="20" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>
                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <DoubleAnimation Storyboard.TargetName="ThumbTransform" Storyboard.TargetProperty="X" To="0" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SliderThumbStyle" TargetType="Thumb">
            <Setter Property="SnapsToDevicePixels" Value="true" />
            <Setter Property="OverridesDefaultStyle" Value="true" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Thumb">
                        <Ellipse Name="Ellipse" Fill="#BB86FC" Stroke="#BB86FC" StrokeThickness="1" Width="14" Height="14" />
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Ellipse" Property="Fill" Value="White" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernSlider" TargetType="Slider">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid>
                            <Border Name="TrackBackground" Background="#333333" Height="4" CornerRadius="2" VerticalAlignment="Center" Margin="5,0" />
                            <Track Name="PART_Track">
                                <Track.Thumb>
                                    <Thumb Style="{StaticResource SliderThumbStyle}" />
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernProgressBar" TargetType="ProgressBar">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="#BB86FC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ProgressBar">
                        <Grid x:Name="TemplateRoot">
                            <VisualStateManager.VisualStateGroups>
                                <VisualStateGroup x:Name="CommonStates">
                                    <VisualState x:Name="Determinate"/>
                                    <VisualState x:Name="Indeterminate">
                                        <Storyboard RepeatBehavior="Forever">
                                            <DoubleAnimationUsingKeyFrames Storyboard.TargetName="Animation" Storyboard.TargetProperty="(UIElement.RenderTransform).(TransformGroup.Children)[0].(ScaleTransform.ScaleX)">
                                                <EasingDoubleKeyFrame KeyTime="0" Value="0.25"/>
                                                <EasingDoubleKeyFrame KeyTime="0:0:1" Value="0.25"/>
                                                <EasingDoubleKeyFrame KeyTime="0:0:2" Value="0.25"/>
                                            </DoubleAnimationUsingKeyFrames>
                                            <PointAnimationUsingKeyFrames Storyboard.TargetName="Animation" Storyboard.TargetProperty="(UIElement.RenderTransformOrigin)">
                                                <EasingPointKeyFrame KeyTime="0" Value="-0.5,0.5"/>
                                                <EasingPointKeyFrame KeyTime="0:0:1" Value="0.5,0.5"/>
                                                <EasingPointKeyFrame KeyTime="0:0:2" Value="1.5,0.5"/>
                                            </PointAnimationUsingKeyFrames>
                                        </Storyboard>
                                    </VisualState>
                                </VisualStateGroup>
                            </VisualStateManager.VisualStateGroups>
                            <Border Background="{TemplateBinding Background}" CornerRadius="7"/>
                            <Rectangle x:Name="PART_Track"/>
                            <Grid x:Name="PART_Indicator" HorizontalAlignment="Left">
                                <Border x:Name="Indicator" Background="{TemplateBinding Foreground}" CornerRadius="7"/>
                                <Border x:Name="Animation" Background="{TemplateBinding Foreground}" CornerRadius="7" RenderTransformOrigin="0.5,0.5">
                                    <Border.RenderTransform>
                                        <TransformGroup>
                                            <ScaleTransform/>
                                            <SkewTransform/>
                                            <RotateTransform/>
                                            <TranslateTransform/>
                                        </TransformGroup>
                                    </Border.RenderTransform>
                                </Border>
                            </Grid>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ToolTip">
            <Setter Property="Background" Value="#252525"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#BB86FC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="HasDropShadow" Value="True"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToolTip">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}"
                                MaxWidth="400">
                            <ContentPresenter />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Window.Icon>
        <StaticResource ResourceKey="SpiderIcon"/>
    </Window.Icon>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#1E1E1E" Padding="15,10" BorderBrush="#333333" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <Image Source="{StaticResource SpiderIcon}" Width="24" Height="24" Margin="0,0,10,0"/>
                    <TextBlock Text="MALSpider" FontSize="20" FontWeight="Bold" Foreground="#BB86FC" VerticalAlignment="Center" Margin="0,0,30,0"/>
                </StackPanel>

                <Grid Grid.Column="1" VerticalAlignment="Center">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="Search or URL:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#B0B0B0" FontSize="14"/>
                    <ComboBox x:Name="SearchBox" Grid.Column="1" Style="{StaticResource ModernComboBox}" IsEditable="True" KeyDown="SearchBox_KeyDown" HorizontalAlignment="Stretch"/>
                    <Button x:Name="SpiderButton" Grid.Column="2" Click="Spider_Click" Style="{StaticResource ModernButton}" Margin="15,0,0,0" Padding="25,8">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{StaticResource SpiderIcon}" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="CRAWL" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="StopButton" Grid.Column="2" Click="StopButton_Click" Style="{StaticResource ModernButton}" Margin="15,0,0,0" Padding="25,8" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal">
                            <Rectangle Fill="White" Width="12" Height="12" Margin="0,0,8,0"/>
                            <TextBlock Text="STOP" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BackButton" Grid.Column="2" Click="BackButton_Click" Style="{StaticResource ModernButton}" Margin="15,0,0,0" Padding="15,8" Visibility="Collapsed" Background="#004D40">
                        <StackPanel Orientation="Horizontal">
                            <Path Data="M20,11H7.83l5.59-5.59L12,4l-8,8l8,8l1.41-1.41L7.83,13H20V11z" Fill="White" Stretch="Uniform" Width="16" Height="16" Margin="0,0,8,0"/>
                            <TextBlock Text="BACK TO MAIN" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="RefreshButton" Grid.Column="3" Click="RefreshButton_Click" Style="{StaticResource ModernButton}" Margin="10,0,0,0" Padding="12,8" ToolTip="Clear cache and re-crawl" IsEnabled="False">
                        <Path Data="M17.65,6.35C16.2,4.9 14.21,4 12,4c-4.42,0-7.99,3.58-7.99,8s3.57,8 7.99,8c3.73,0 6.84,-2.55 7.73,-6h-2.08c-0.82,2.33,-3.04,4,-5.65,4c-3.31,0,-6,-2.69,-6,-6s2.69,-6 6,-6c1.66,0 3.14,0.69 4.22,1.78L13,11h7V4l-2.35,2.35z"
                              Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=Button}}"
                              Stretch="Uniform" Width="18" Height="18"/>
                    </Button>
                </Grid>
            </Grid>
        </Border>

        <Grid Grid.Row="1">
            <!-- Fixed Sidebar for Toggles -->
            <Border Width="120" HorizontalAlignment="Left" Background="#B31E1E1E" BorderBrush="#333333" BorderThickness="0,0,1,0" Panel.ZIndex="100">
                <Grid>
                    <StackPanel Margin="10,25">
                        <TextBlock Text="VISIBLE LANES" FontSize="11" FontWeight="Bold" Foreground="#666666" Margin="0,0,0,25" HorizontalAlignment="Center"/>
                        <CheckBox x:Name="LnToggle" Style="{StaticResource ModernToggleSwitch}" Content="Light Novel" Margin="0,0,0,30" IsChecked="True" Click="LaneToggle_Click"/>
                        <CheckBox x:Name="MangaToggle" Style="{StaticResource ModernToggleSwitch}" Content="Manga" Margin="0,0,0,30" IsChecked="True" Click="LaneToggle_Click"/>
                        <CheckBox x:Name="AnimeToggle" Style="{StaticResource ModernToggleSwitch}" Content="Anime" IsChecked="True" Click="LaneToggle_Click"/>
                    </StackPanel>

                    <Border VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,20" Width="26" Height="26" CornerRadius="13" Background="#333333" Cursor="Help"
                            ToolTipService.InitialShowDelay="0" ToolTipService.ShowDuration="60000">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#BB86FC"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <ToolTipService.ToolTip>
                            <ToolTip Background="#252525" BorderBrush="#BB86FC" BorderThickness="1">
                                <StackPanel Margin="5">
                                    <TextBlock Text="CONTROLS" FontWeight="Bold" Margin="0,0,0,10" Foreground="#BB86FC"/>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="20"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <TextBlock Text="LMB Drag" Grid.Row="0" Grid.Column="0" Foreground="#B0B0B0"/>
                                        <TextBlock Text="Pan graph" Grid.Row="0" Grid.Column="2"/>

                                        <TextBlock Text="LMB Click" Grid.Row="1" Grid.Column="0" Foreground="#B0B0B0"/>
                                        <TextBlock Text="Open MAL page" Grid.Row="1" Grid.Column="2"/>

                                        <TextBlock Text="Ctrl + Wheel" Grid.Row="2" Grid.Column="0" Foreground="#B0B0B0"/>
                                        <TextBlock Text="Zoom in/out" Grid.Row="2" Grid.Column="2"/>

                                        <TextBlock Text="F" Grid.Row="3" Grid.Column="0" Foreground="#B0B0B0"/>
                                        <TextBlock Text="Focus current node" Grid.Row="3" Grid.Column="2"/>

                                        <TextBlock Text="Ctrl + LMB" Grid.Row="4" Grid.Column="0" Foreground="#B0B0B0"/>
                                        <TextBlock Text="Isolate subgraph" Grid.Row="4" Grid.Column="2"/>

                                        <TextBlock Text="Side Button 1" Grid.Row="5" Grid.Column="0" Foreground="#B0B0B0"/>
                                        <TextBlock Text="Back to main view" Grid.Row="5" Grid.Column="2"/>
                                    </Grid>
                                </StackPanel>
                            </ToolTip>
                        </ToolTipService.ToolTip>
                        <Grid>
                            <TextBlock Text="?" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontWeight="Bold" FontSize="14" Margin="0,0,0,1">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                                <Setter Property="Foreground" Value="Black"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                </Grid>
            </Border>

            <ScrollViewer x:Name="MainScrollViewer" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                          PreviewMouseDown="ScrollViewer_MouseDown" PreviewMouseMove="ScrollViewer_MouseMove" PreviewMouseUp="ScrollViewer_MouseUp"
                          PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                          ScrollChanged="MainScrollViewer_ScrollChanged">
                <Canvas x:Name="GraphCanvas" Background="#121212" RenderTransformOrigin="0,0">
                    <Canvas.LayoutTransform>
                        <ScaleTransform x:Name="GraphScale" ScaleX="1.0" ScaleY="1.0"/>
                    </Canvas.LayoutTransform>
                </Canvas>
            </ScrollViewer>

            <Canvas x:Name="LoadingCanvas" Visibility="Collapsed" IsHitTestVisible="False" Background="#121212" ClipToBounds="True"/>
            <Grid HorizontalAlignment="Right" Width="55" IsHitTestVisible="False" ClipToBounds="True" Background="#B3121212">
                <Grid.Style>
                    <Style TargetType="Grid">
                        <Setter Property="Margin" Value="0,80,12,0"/>
                        <Style.Triggers>
                            <!-- Combination: Both Visible -->
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding ComputedVerticalScrollBarVisibility, ElementName=MainScrollViewer}" Value="Visible"/>
                                    <Condition Binding="{Binding ComputedHorizontalScrollBarVisibility, ElementName=MainScrollViewer}" Value="Visible"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Margin" Value="0,80,12,12"/>
                            </MultiDataTrigger>
                            <!-- Combination: Only Horizontal Visible -->
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding ComputedVerticalScrollBarVisibility, ElementName=MainScrollViewer}" Value="Collapsed"/>
                                    <Condition Binding="{Binding ComputedHorizontalScrollBarVisibility, ElementName=MainScrollViewer}" Value="Visible"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Margin" Value="0,80,0,12"/>
                            </MultiDataTrigger>
                            <!-- Combination: Both Collapsed -->
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding ComputedVerticalScrollBarVisibility, ElementName=MainScrollViewer}" Value="Collapsed"/>
                                    <Condition Binding="{Binding ComputedHorizontalScrollBarVisibility, ElementName=MainScrollViewer}" Value="Collapsed"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Margin" Value="0,80,0,0"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </Grid.Style>
                <Canvas x:Name="TimeCanvas">
                    <Canvas.RenderTransform>
                        <TranslateTransform x:Name="TimeTransform"/>
                    </Canvas.RenderTransform>
                </Canvas>
            </Grid>
        </Grid>

        <StatusBar Grid.Row="2" Background="#1A1A1A" BorderBrush="#333333" BorderThickness="0,1,0,0">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0" Margin="10,0,0,0">
                <TextBlock x:Name="StatusLabel" Foreground="#BB86FC" TextTrimming="CharacterEllipsis" FontSize="13"/>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <StackPanel Orientation="Horizontal" Margin="0,0,10,0">
                    <TextBlock Text="Zoom:" VerticalAlignment="Center" Foreground="#B0B0B0" FontSize="12" Margin="0,0,5,0"/>
                    <Slider x:Name="ZoomSlider" Width="120" VerticalAlignment="Center"
                            Minimum="{x:Static local:MALSpiderConstants.MinZoom}"
                            Maximum="{x:Static local:MALSpiderConstants.MaxZoom}"
                            Value="1.0"
                            SmallChange="0.05" LargeChange="0.1"
                            Style="{StaticResource ModernSlider}"
                            ValueChanged="ZoomSlider_ValueChanged"/>
                    <TextBlock Text="{Binding Value, ElementName=ZoomSlider, StringFormat={}{0:P0}}"
                               Width="40" VerticalAlignment="Center" Foreground="#BB86FC" FontWeight="Bold" FontSize="12" Margin="5,0,0,0"/>
                    <Button Content="Reset" Click="ResetZoom_Click" Style="{StaticResource ModernButton}" Padding="8,2" FontSize="11" Margin="5,0,0,0"/>
                </StackPanel>
            </StatusBarItem>
            <StatusBarItem Grid.Column="2" Margin="0,0,10,0">
                <ProgressBar x:Name="WorkProgressBar" Width="150" Height="14" Visibility="Collapsed" Minimum="0" Maximum="100" Style="{StaticResource ModernProgressBar}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
